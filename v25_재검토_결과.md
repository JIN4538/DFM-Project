# v2.5 재검토 — 냉정한 판정

검토일 2026-09-09 · 대상 `AMDFM_v2 2.zip` (dfm2, v2.5) + `AM_DFM_v25_리뷰_검토와_수정_결과.docx`

검토 방법: v2.4와 v2.5를 같은 환경에 동시에 설치하고 **동일 입력으로 나란히 실행**해 수치를 직접 대조. 전체 테스트 재실행, 신규 모듈 정적 검토, 고속 경로와 일반 경로의 결과 일치 검증, 프로파일링.

---

## 0. 총평

**세 가지 지적 모두 실제로 고쳐졌고, 독립 재현으로 확인했다.** 고친 방식이 내가 제안한 것보다 나은 부분이 있다.
문서에서 반박한 세 지점(2A/P 용어, 10~50배 속도, 전체 면적 비율)도 반박이 맞다. 내 쪽이 틀렸다.

남은 문제는 하나가 실질적이다: **표시 하한 정책을 세 검사 중 하나에만 적용했다.**

| 테스트 통과 | 149개 (주장과 정확히 일치, 재현 확인) |
|---|---|
| 기존 수치 보존 | 부피·지지·한층 합계 v2.4와 상대오차 1e-11 이내 |
| 신규 회귀 | 발견 없음 |

---

## 1. 지적 3건의 처리 — 검증 결과

### (1) 오검출 — 고쳐짐, 방식이 더 낫다

`classify_thin_regions`가 후보를 지우지 않고 `major`/`minor`로 나누고, 하한 미만이어도 **성분 전체가 소실되면 우선 검토로 승격**(`whole_component_lost`)한다. 내가 제안한 단순 면적 하한은 작은 실제 특징을 지웠을 것이다. 이쪽이 맞다.

| 형상 | v2.4 | v2.5 |
|---|---|---|
| 구 r25 (250층) | risk, 110개 층 | **details_only, 0개 층** (세부 8.5e-4mm² 별도 보존) |
| **NIST 시험편** | **risk, 85개 층, 6.113786mm²** | **risk, 20개 층, 6.113452mm²** |
| 0.3mm 벽 | risk 유지 | risk 유지, 면적 동일 |
| 지름 0.03mm 핀 | — | risk 유지 (층당 7e-4mm², 하한 미만인데 성분 전체 소실로 승격) |

NIST 기준 **오검출 층 76% 감소, 검출 면적의 99.995% 보존.** 이게 이번 수정의 가장 강한 증거다.

### (2) 감김 수 한도 — 고쳐짐

`WindingIndex`가 STRtree로 수평 레이와 bbox가 겹치는 선분만 후보로 삼는다. 부호 판정식은 그대로라 결과가 바뀌지 않는다.

| 형상 | v2.4 | v2.5 |
|---|---|---|
| 900개 핀 (57,600면) | 한도 초과, 층 폐기 | **완료 0.60초**, 27,000회 (전수라면 25,920,000회 → **960배 감소**) |
| 1,600개 핀 (102,400면) | (더 심함) | 완료 1.29초, 64,000회 |

면적도 정확하다: 900핀 991.92mm² = 16각형 내접 정확값(1017.88 × 0.9745).

### (3) 층 단위 all-or-nothing — 조건부로 고쳐짐, 안전 조건이 정확하다

`isolated_material`이 손상 범위 bbox와 **전이적으로** 접촉하지 않는 닫힌 성분만 회수한다.

| 형상 | 결과 |
|---|---|
| 손상 구 + 100mm 떨어진 정상 박스 | 전체 확정 85/200 유지, **재료 확인 층 200/200**, 지지 검사가 200개 층 측정·11개 층 위험 검출 |
| 좋은 외곽 안에 **손상된 공동** | `known_area = 0` — 감싸는 외곽 링까지 제외 |
| 손상 부품 + 멀리 떨어진 정상 박스 | `known_area = 100.0mm²` 정확히 회수 |

두 번째 줄이 핵심이다. 손상된 공동을 감싸는 외곽을 회수했다면 **구멍을 채운 채로 확정**하는 심각한 오류가 됐을 텐데, bbox 포함 관계로 정확히 막았다. 부분 회수에서 제일 위험한 지점을 제대로 잡았다.

### (4) 빈 재료 공허한 "이상 없음" — 고쳐짐

껍질 중복+반전 → `status=unavailable`, 명시적 사유, **네 검사 모두 unresolved**. v2.4는 세 검사가 "미검출"이었다.

### (5) 고속 경로 정확성 — 이상 없음

`fast_linework`(끝점 루프)와 일반 노딩 경로를 같은 입력으로 대조:

| 형상 | 경로 | 면적 | 정답 | 노딩 경로와 |
|---|---|---|---|---|
| 20mm 박스 + 8mm 공동 (중첩 링) | endpoint_loops | 336.00000 | 336 | 일치 |
| 3중 중첩 껍질 | endpoint_loops | 268.00000 | 268 | 일치 |
| 모서리에서 닿는 박스 2개 | noded (폴백) | 200.00000 | 200 | 일치 |
| 면을 공유하는 박스 2개 (seam) | noded (폴백) | 200.00000 | 200 | 일치 |
| 교차하는 막대 2개 | noded (폴백) | 224.00000 | 224 | 일치 |

중첩 링은 고속 경로로 처리하고, 닿거나 교차하면 정확히 폴백한다. 조건(모든 성분이 단순 닫힌 링 + 성분 간 비교차)이 올바르다.

---

## 2. 문서의 반박 — 전부 타당

| 반박 | 판정 |
|---|---|
| 2A/P는 국소 벽두께가 아니라 형상 크기 (원에서 반지름) | **맞다. 내 표현이 틀렸다.** 코드 주석과 UI 캡션에 정확히 반영돼 있음 |
| 10~50배 속도는 보장 못 함 | **맞다. 실측 약 2배다** (아래) |
| 전체 추정 면적으로 비율을 만들지 않음 | **맞다.** 미확정 상태에서 분모를 지어내는 것이 됐을 것 |
| 작은 성분과 후보 집계는 보존해야 함 | 맞다. 삭제형 하한이었다면 0.03mm 핀을 놓쳤을 것 |

내 속도 추정이 빗나간 이유: 노딩을 항상 우회할 수 있다고 가정했는데, 고속 경로는 조건부이고 노딩이 사라지자 병목이 shapely의 `difference`/`buffer`로 옮겨갔다.

---

## 3. 성능 — 실측 (같은 머신, 동일 입력)

| 형상 | v2.4 | v2.5 | 배율 |
|---|---|---|---|
| icosphere sub4 (5,120면, 250층) | 2.69s | 1.55s | 1.74× |
| icosphere sub5 (20,480면) | 4.84s | 2.63s | 1.84× |
| icosphere sub6 (81,920면) | 9.83s | 5.01s | 1.96× |
| NIST 시험편 | 4.88s | 2.41s | 2.02× |
| 900개 핀 단면 1장 | 분석 불가 | 0.60s | — |

일관되게 약 2배. 정직한 수치다.

---

## 4. 남은 문제

### (A) 표시 하한을 세 검사 중 하나에만 적용했다 — 실질적

`classify_thin_regions`의 `floor = 0.01·선폭²`는 **선폭 검사에만** 적용된다. 나머지 두 검사는 여전히 격자 기준 `meaningful_regions`만 쓴다. NIST 시험편 실측:

| 검사 | 위험 층 | 최소 면적 | 중앙값 | 최대 | 표시 하한 미만 층 |
|---|---|---|---|---|---|
| 선폭 (수정됨) | 20 | 1.9e-01 | 3.1e-01 | 4.2e-01 | **0 / 20** |
| 지지·브리지 | 15 | 5.0e-06 | 1.4e+00 | 3.2e+01 | 1 / 15 |
| **한 층에만 나타나는 영역** | **61** | **2.6e-07** | **6.5e-05** | **7.1e-04** | **61 / 61** |

즉 "한 층에만 나타나는 영역"의 NIST 검출 61개 층은 **전부 하한 미만**이고, 합계 0.00414mm²다. v2.4와 완전히 동일한 종류의 오검출이 이 검사에는 그대로 남아 있다. 화면에서는 "검토 영역 있음"으로 표시된다.

수정은 같은 방식이면 된다. `unsupported`와 `single_layer` 결과도 `major`/`minor`로 분류하고, 승격 조건은 `면적 ≥ floor 또는 성분 전체`로 두면 자연스럽다 — 작은 조각 전체가 지지되지 않는 것은 크기와 무관하게 실제 위험이므로 승격 규칙이 그대로 성립한다.

### (B) 층마다 `mesh.extents`를 다시 읽는다 — 한 줄, 1.36배

`section_mesh` 첫머리의 `scale = float(np.max(mesh.extents))`가 층마다 trimesh 캐시 검증을 유발하고, 그게 정점 배열 전체를 해싱한다.

프로파일(82k 구, 250층, 총 5.02초):

| 항목 | 시간 | 비중 |
|---|---|---|
| `shapely.difference` | 1.52s | 26% |
| **`trimesh.caching.hash_fallback`** | **1.20s** | **21%** |
| `simple_ring` (파이썬 인접 리스트) | 0.30s | 5% |

`extents`를 루프 밖에서 한 번 구해 넘기도록 바꾸면 **5.02s → 3.69s (1.36배)**. 실제로 패치해서 측정한 값이다.

### (C) 병목이 옮겨갔다 — 다음 최적화 대상은 단면이 아니다

82k 구 1방향 평가: 기존 3D 분석만 7.95초, 단면 포함 10.50초. **단면 검토는 이제 24%다.**
6방향 비교 버튼은 73.6초인데 대부분 기존 레이캐스팅 벽두께 분석이다. 단면을 더 빠르게 해도 이 버튼은 별로 안 빨라진다.

### (D) 심하게 겹친 경계는 여전히 누적 한도에 걸린다 — 문서에 명시됨, 낮은 우선순위

| 형상 | 결과 |
|---|---|
| 겹친 원기둥 400개 (38,400면) | 완료, 596,722회 (전수 189,638,400회 대비 318배 감소) |
| 겹친 원기둥 1,200개 (115,200면) | **10.8초 소모 후 한도 초과** (전수라면 50억 회) |

실패는 사유를 남기고 안전하게 처리되며(미확정 범위를 전역으로 두어 인접 층 오검출 차단), 문서도 이 한계를 명시했다. 다만 10.8초를 쓰고 실패하는 것은 아깝다. 후보 수를 먼저 추정해 조기에 중단하면 된다.

### (E) 사소 — `except WindingLimit` 블록의 잠재 NameError

`sections.py`의 함수 레벨 `except WindingLimit`이 `index.tests`를 참조하는데, 현재는 `index` 바인딩 이후에만 도달 가능해서 문제가 없다. 다만 위쪽에 `WindingLimit`을 던질 수 있는 코드가 추가되면 조용히 `NameError`가 된다. `index=None` 초기화 한 줄로 막을 수 있다.

### (F) 원리적 한계 — 고칠 수 없음, 문서화로 충분

내부 껍질이 중복+반전된 경우 그 영역이 조용히 사라진다. 경계 방향 균형이 0이고 잔여 선분도 0이라 진단으로 잡히지 않는다. 그런데 이 신호는 **면을 공유하는 정상 솔리드 두 개**와 구분되지 않는다(실제로 그 경우 200mm²가 정확히 나온다). nonzero 규칙의 본질적 모호성이고 v2.4 문서에 이미 적혀 있다. Cura 대조에서만 드러난다.

---

## 5. 권장 순서

1. **(A)** 지지·한층 검사에도 major/minor 분류 적용 — 남은 오검출의 전부이고, 이미 만든 함수를 재사용하면 된다
2. **(B)** `extents` 루프 밖으로 — 한 줄, 1.36배
3. **Cura 대조** — 이제 여기가 병목이다. 코드 쪽 개선은 수확체감 구간에 들어왔고, "값의 제조적 의미"에 대한 근거가 하나도 없다는 점이 남은 가장 큰 약점이다
4. (C) UI 응답성이 문제면 기존 3D 레이캐스팅 분석을 봐야 한다 (단면 아님)
5. (D)(E)는 여유 있을 때

---

## 6. 판정

v2.4→v2.5는 잘 된 수정이다. 지적을 그대로 받아 적은 게 아니라 **각각의 부작용을 따져 더 나은 형태로 구현**했고(성분 전체 소실 승격, bbox 전이 오염, 분모 만들지 않기), 반박한 세 지점도 전부 맞다. 기존 결과를 1e-11 이내로 보존한 것도 확인했다.

다만 (A)는 방금 고친 것과 **완전히 같은 종류의 문제**가 다른 두 검사에 남은 것이라, 이번에 함께 처리하지 않은 것은 누락으로 봐야 한다. NIST 61개 층이 근거다.

그리고 코드 성숙도에 비해 **제조적 검증이 여전히 0**이다. 지금 소프트웨어가 내는 숫자가 실제 제조 위험과 얼마나 대응하는지에 대한 증거는 한 건도 없고, 이건 코드를 더 고쳐서 해결되지 않는다. 다음 사이클은 코드가 아니라 Cura 대조와 라벨 세트여야 한다.
