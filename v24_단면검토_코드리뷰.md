# v2.4 단면 검토(sections.py / layer_review.py) 코드 검토

검토일 2026-09-09 · 대상 `AMDFM_v2 1.zip` (dfm2, v2.4)

검토 방법: 정적 읽기 + 실행 검증 (pytest 113 passed, 추가로 합성 파손 메시 20종 · 성능/스케일링 측정)

---

## 1. 결론

접근 자체는 타당하다. 슬라이서가 실제로 하는 일(삼각형-평면 교차 → 평면에서 윤곽 구성 → 부호 있는 감김 수로 재료 판정)과 같은 구조이고,
"닫힌 솔리드"를 요구하는 3D 게이트와 단면 검토를 분리한 것이 원래 문제(열린 STL에서 검토 자체가 거부됨)의 정확한 해법이다.

실행으로 확인한 결과:

| 입력 | 3D 종합 점수 | 단면 검토 |
|---|---|---|
| 뚜껑 없는 상자 (열린 메시) | 판정 보류 (기존과 동일) | complete, 커버리지 100%, 부피 4000mm³ |
| 삼각형 1개 삭제 (구 5120면) | 판정 보류 | partial, 커버리지 98% |
| 삼각형 3개 삭제 | 판정 보류 | partial, 커버리지 92.5% |
| 삼각형 10개 삭제 | 판정 보류 | partial, 커버리지 76% |
| 삼각형 30개 삭제 | 판정 보류 | partial, 커버리지 38.5% |
| 서로 관통하는 상자 2개 | — | complete, 부피 6628mm³ (겹침 중복 계산 없음) |
| 면 맞닿은 상자 2개 (내부 seam) | — | complete, 면적 800mm² 정확 |
| 전면 법선 반전 | — | complete, 부피 4000mm³ 정상 |

구현 디테일도 대체로 옳다.

- 정점 인덱스를 정렬해 교차점을 계산하므로 인접 두 삼각형에서 **비트 단위로 동일한 끝점**이 나온다. CuraEngine/Slic3r와 같은 방식이고, 틈이 생기지 않는 이유가 여기 있다.
- 반개구간 부호(`>= 0`)로 정점이 평면에 정확히 놓인 경우를 중복 없이 처리한다.
- `winding_at`은 표준 Sunday 감김 수 알고리즘이고 구현이 맞다.
- 전면 반전 메시도 부호 무관 nonzero 규칙이라 정상 판정된다.
- 수리하지 않고 보고만 한다는 원칙이 코드 전반에서 실제로 지켜진다.

다만 **실사용에서 바로 문제가 되는 결함이 3개** 있고, 전부 고칠 수 있다.

---

## 2. 반드시 고쳐야 할 것

### (1) 위험 판정에 크기 기준이 없다 — 정상 부품에서 오검출

`meaningful_regions`의 임계값이 좌표 격자(grid) 기준이라 사실상 아무것도 걸러내지 못한다.

```python
p.area > max(8*grid*p.length, grid*grid*16)   # grid ≈ 2.5e-10 mm
# 둘레 1mm 영역 기준 임계값 = 2e-9 mm²
```

실측 결과:

| 입력 | 검출 영역 수 | 면적 |
|---|---|---|
| 매끈한 구 r25 (z=-2.5 층) | 16개 | 6.6e-08 ~ 1.4e-05 mm² |
| 구 전체 250층 | 28개 층에서 risk | 합계 8.5e-04 mm² |
| **실제 0.3mm 벽** | 1개 | **6 mm²** |

노이즈와 실제 신호가 **5~9자리** 차이난다. 그런데 `check()`가 `r[key] > 0`으로만 판정하므로 매끈한 구가 "선폭 기준 얇은 단면 특징: 위험"으로 표시된다.
NIST 시험편도 85개 층 / 6.1mm²가 잡히는데 상당 부분이 이 노이즈로 보인다.

원인은 이산화된 곡면의 준-공선 정점들이 mitre 오프셋에서 슬리버를 만드는 것. `simplify(grid*2)`는 tolerance가 1e-9라 제거하지 못한다.

**수정 방향**

- `meaningful_regions`에 물리 단위 하한을 추가: `p.area > max(기존, 0.01 * line_width**2)` (= 0.0016mm²). 위 노이즈 100% 제거, 실제 0.3mm 벽 100% 유지.
- 더 낫게는 영역의 **국소 폭**(2·area/perimeter)을 함께 보고해서 "가는 모서리(폭 작음·면적 작음)"와 "얇은 벽(폭 작음·면적 큼)"을 구분한다. 지금은 REVIEW_KO.md에서 글로 구분하지만 수치로는 구분이 안 된다.
- UI의 `default=max(..., thin_area_mm2)`도 노이즈 층으로 점프하므로 같이 고쳐야 한다.

### (2) 감김 수 판정이 O(셀 수 × 선분 수) — 이차 스케일링 절벽

`winding_tests = len(cells) * len(snapped)`가 `max_winding_tests=8e6`를 넘으면 **그 층 전체를 버린다.**

측정:

| 핀 개수 | 면 수 | 선분 | 셀 | 결과 |
|---|---|---|---|---|
| 100 | 6,400 | 3,200 | 100 | 320K, 정상 |
| 400 | 25,600 | 12,800 | 400 | 5.12M, 정상 |
| **900** | **57,600** | **28,800** | **900** | **25.9M → 한도 초과, 층 폐기** |

5.7만 면은 인터넷 STL로 전혀 큰 편이 아니다. 격자/라티스/로고 문자 같은 형상은 바로 걸린다.

**수정 방향**

- 선분에 STRtree 또는 y-구간 정렬 + `np.searchsorted`를 적용해 각 셀의 대표점이 실제로 교차할 수 있는 선분만 테스트한다 → 거의 선형.
- 또는 `polygonize` 결과의 포함 관계 트리를 만들어 최외곽 링에서만 감김 수를 계산하고 중첩 깊이로 전파한다.
- 두 방식 모두 결과는 동일하고 한도 초과 자체를 없앨 수 있다.

### (3) 층 단위 all-or-nothing — 결함 1개가 그 층 전체를 무효화

한 층에 열린 선분이 하나라도 남으면 그 층의 **모든** 지표가 미확정이 된다. 결함에서 멀리 떨어진 정상 영역도 같이 버려진다.
그래서 삼각형 30개(전체의 0.6%) 손상만으로 커버리지가 38%로 떨어진다. 결함 수백 개인 실제 파손 STL은 거의 0%가 된다.

**수정 방향**

- `polygonize_full`의 `cuts`/`dangles`가 속한 연결 성분만 "미확정 영역"으로 표시하고, 나머지 닫힌 성분에 대해서는 지표를 계산한다.
- 층별 `complete` 불리언을 "확정 면적 / 전체 추정 면적" 비율로 바꾸면, 부분 손상 모델에서도 수치가 나온다.
- 이게 이번 버전에서 가장 효과가 큰 구조 개선이다. (1)(2)보다 작업량은 크다.

---

## 3. 성능

`intersect_triangles`가 층마다 **전체 정점**에 대해 `(vertices - origin) @ normal`을 다시 계산한다. 빌드 프레임에서는 법선이 [0,0,1]이므로 `vertices[:,2]`를 한 번만 구하면 된다.

| 모델 | 면 수 | 층 | 1방향 시간 | 6방향 추정 |
|---|---|---|---|---|
| icosphere sub4 | 5,120 | 250 | 3.1s | ~19s |
| icosphere sub5 | 20,480 | 250 | 5.9s | ~35s |
| icosphere sub6 | 81,920 | 250 | 12.3s | ~74s |
| NIST 시험편 | 7,392 | ~100 | 6.1s | ~37s |

프로파일: `shapely.union_all(linestrings, grid_size=grid)`가 전체의 **53%**. 선분 노딩이 지배적이다.

**수정 방향**

- 끝점이 이미 비트 단위로 일치하므로, 끝점 해시로 윤곽을 직접 연결(O(n))하고 **연결에 실패한 경우에만** shapely 노딩으로 폴백한다. 정상 메시에서는 shapely 호출이 거의 사라진다. 10~50배 기대.
- z 방향으로 삼각형을 버킷팅해서 층마다 O(F) 스캔을 없앤다.
- `scoring.evaluate_orientations`는 6방향 전부 단면 검토를 돌린다. UI에서 방향 비교는 옵트인으로 두거나 캐시가 필요하다.
- `ui/app.py::render_layer_review`는 슬라이더/셀렉트박스를 만질 때마다 `section_mesh`를 `local_faces` 없이 재계산한다. `@st.cache_data` 필요.

---

## 4. 사소하지만 고쳐둘 것

- **빈 단면의 공허한 "이상 없음"**: 껍질이 중복되고 하나가 반전된 메시는 모든 층 면적이 0인데, `단면 구성`만 unresolved이고 나머지 3개 검사는 `clear_in_layers`("미검출")로 나온다. UI는 status='unavailable'이라 조기 반환하지만 CLI/JSON에는 그대로 나간다. 면적이 0인 층은 다른 검사도 `unresolved`로 두는 게 맞다.
- 같은 경우 `report['reason']`이 설정되지 않아 UI가 일반 메시지를 띄운다. "모든 층의 재료 면적이 0 — 면 방향 상쇄 가능성" 같은 구체적 사유가 필요하다.
- `inspect_layers`는 winding이 뒤섞인 메시에서 커버리지 0%가 된다. 앱 경로는 `model_loader`의 `trimesh.repair.fix_winding`이 살려주지만(실측 확인), 모듈을 직접 호출하면 그대로 실패한다. `inspect_layers` 안에서 winding 불일치를 감지해 명시적으로 안내하는 편이 낫다.

---

## 5. 검증 관점 (교수님 대응)

REVIEW_KO.md의 "다음 개선 순서"는 방향이 맞다. 다만 지금 근거는 **완료율**뿐이고, 완료율은 정확도가 아니다. 다음 단계에서 필요한 것:

- 동일 STL·동일 선폭/층 높이/방향으로 Cura에서 슬라이싱하고, 대표 층의 재료 윤곽 면적을 직접 비교 (상대오차 %로 보고)
- 라벨링된 형상 세트에 대해 **정밀도/재현율**을 측정. 특히 (1)을 고치기 전후로 오검출률이 얼마나 떨어지는지가 그대로 기여도가 된다
- 커버리지는 "처리 완료율"로만 부르고 "검토 정확도"로 부르지 않기 (현재 문서는 이미 구분하고 있음 — 유지)

---

## 6. 권장 작업 순서

1. `meaningful_regions`에 물리 단위 하한 + 국소 폭 보고 — 몇 줄, 효과 즉시 (오검출 제거)
2. 감김 수 판정 공간 인덱싱 — 한도 초과 실패 제거
3. 끝점 해시 기반 윤곽 연결 + z 버킷팅 — 속도
4. 층 내 부분 확정(연결 성분 단위) — 구조 개선, 파손 STL 커버리지
5. Cura 대조 실측 — 검증
